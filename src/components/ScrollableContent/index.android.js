import React from 'react';
import ScrollableContentView from './ScrollableContent';

const ScrollableContent = ({ movie, children }) => <ScrollableContentView movie={movie} children={children} />;

export default ScrollableContent;
