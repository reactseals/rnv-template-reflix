import { StyleSheet } from 'react-native';
import colors from '../../../res/colors';

export default StyleSheet.create({
  category: {
    color: colors.text,
    fontSize: 28,
    fontWeight: '700',
  },
  listWrapper: {
    marginTop: 10,
    marginBottom: 5,
    marginHorizontal: 20,
  },
});
